export REPOS_DIR="${HOME}/repos"

function repos() {
  cd $REPOS_DIR || false
  cd "$(fd --type d --max-depth 1 | fzf)" || false
}

function vscode() {
  cd $REPOS_DIR || exit
  code "$(fd --type d --max-depth 1 | fzf)"
}
